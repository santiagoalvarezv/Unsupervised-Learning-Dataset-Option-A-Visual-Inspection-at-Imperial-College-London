numpy
Pillow
scikit-learn
matplotlib
joblib

# Needed for --method autoencoder (convolutional) and --method cnn (deep features).
# Skip both lines below if you only ever use --method pca.
torch
torchvision
